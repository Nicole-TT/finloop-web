# AGENTS.md

本文档用于约束本项目中的 AI / 自动化开发行为。开发时优先遵循本文件，其次遵循用户当前消息。

## 基本原则

- 先读现有代码、页面内容和素材，再动手修改，优先沿用项目已有结构和写法。
- 保持实现直接、可读，能用现有能力解决就不要引入复杂抽象或新的框架。
- 每次写完代码，不需要检查语法，不需要执行构建，用户会自己做。
- 不要改无关文件，不要顺手重构；只在完成当前需求所需的范围内修改。
- 如果工作区已有用户改动，不要回滚、覆盖或删除；在现有改动基础上追加修改。
- 新增依赖前先确认现有依赖无法满足需求，并同步更新 `package.json` 与锁文件。
- 页面文案、业务事实和品牌名称以项目资料或用户明确提供的信息为准，不要自行编造资质、客户、数据或承诺。

## 任务规模与执行流程

- 每次收到修改请求，先按当前改动本身判断规模，不要沿用上一轮整站开发或完整验收流程。
- 微小修改包括：单处文字、颜色、字号、间距、图标、链接或明确选择器下的局部样式。此类任务直接定位源代码并修改，完成后立即回复。
- 微小修改不得自动执行生产构建、完整页面截图、全站浏览器回归、控制台扫描或重复读取需求文档，除非用户明确要求。
- 局部布局或交互修改只检查受影响区域；不要把局部修改升级为全站审查。
- 只有涉及依赖、构建配置、资源路径、跨页面公共结构、复杂响应式布局或发布准备时，才扩大验证范围。
- 已读取且未发生变化的 `AGENTS.md`、技能说明和需求文档无需在每次小修改前重复读取。
- 浏览器标注已经提供目标元素、选择器和具体属性时，优先直接修改拥有该样式的源代码或设计变量，不重复定位和分析已明确的信息。
- 回复内容与任务规模匹配。微小修改只需说明改了什么，不复述完整流程。


## 前端规范

- 前端使用 Vite、Lucide 图标、React 不要无必要地引入Vue 或其他 UI 框架。
- 页面入口为 `index.html`，应用渲染和交互逻辑主要放在 `src/main.js`，全局样式放在 `src/styles.css`。
- 复用现有 CSS 变量、断点、间距、颜色和动效；页面私有样式优先放在 `src/styles.css` 的对应区域，不新增零散样式文件。
- 图标优先使用已安装的 `lucide`，通过现有 `createIcons` 方式注册和渲染，不手写重复 SVG 图标。
- 交互控件必须具备可访问名称、键盘焦点样式和正确的 ARIA 状态；移动端菜单、下拉导航和标签切换要同步更新 `aria-expanded` / `aria-selected` 等属性。
- 保持响应式布局，修改桌面布局后同步检查窄屏；不要让导航、按钮、长文案或图片在移动端溢出或相互遮挡。
- 图片和视频使用项目已有 `assets/` 或 `官网资料/` 素材；新增素材应说明来源，并使用有意义的 `alt` 文本。装饰性图片使用空 `alt`。
- 页面内容以中文为主，英文产品名、品牌名和必要技术术语保持项目现有写法。
- 不要把业务数据、联系方式或大量内容写入浏览器存储；本项目当前以静态展示为主，表单或数据接入需要用户明确要求后再设计。

## 内容与素材规范

- 官网结构和文案修改前，先参考 `Finloop网站内容蓝图_V0.1.md` 及 `官网资料/` 中的已提供材料。
- 对牌照、市场认可、合作关系、客户案例、地址、电话和邮箱等信息保持保守表述；未经核验不要新增确定性声明。
- 产品名、平台分层和业务范围在页面各处保持一致；修改导航或页脚时同步检查对应锚点和内容区块。
- 临时截图、转换文件和审核产物放在 `tmp/`，不要混入正式页面资源目录。

## 代码与文件规范

- 使用 ASCII 作为默认代码字符集；中文文案属于页面内容时按现有文件编码保留。
- JavaScript 逻辑按数据、模板、初始化、事件处理的顺序组织；重复内容优先通过现有数组映射生成，避免复制粘贴。
- 不要在单个函数或模板中堆叠与需求无关的逻辑；复杂交互可拆成同文件内的小函数，但避免为了简单转发而新增组件层。
- 修改资源路径时使用从站点根目录开始的路径，例如 `/assets/...`；确保开发服务器和生产构建都能正确加载。

## 验证规范

- 修改完成后至少检查受影响页面的桌面和移动端布局、主要交互、锚点跳转、图片加载和控制台错误。
- 涉及构建配置、依赖或资源路径时运行 `npm run build`；纯文案或样式小改动可进行针对性检查。
- 不要为验证生成大量临时文件；验证产物统一放入 `tmp/`，并避免提交无关产物。
- 上述完整页面检查适用于结构、布局、交互或发布相关修改；单处文字或视觉属性修改不触发完整检查。
- 不要仅因为通用开发流程、此前使用过的设计技能或上一轮执行过完整验收，就为当前微小修改运行构建或浏览器检查。

## 发布规范

- 发布前确认 `package.json` 中版本号、页面标题、描述、联系方式和资源路径准确。
- 发布前检查生产构建结果，确认不存在外部本地路径、临时素材或未完成的占位文案。
- 未经用户明确要求，不执行发布、部署、域名配置或第三方平台写入操作。

## 项目注意事项

- 当前项目是 Finloop 星路科技官网静态前端，不包含后端 API、数据库或登录系统。
- 页面中的资质、牌照、合作关系和市场认可内容必须以可核验资料为依据；不确定时使用保守描述或保留待审核状态。
- 当前导航中的部分资源中心、法律信息和产品链接是展示性锚点，新增真实页面或外链前需确认目标地址和内容已准备好。
- `tmp/` 下的图片和文档页面是内容审核辅助材料，不应直接作为官网正式内容引用。
