# Finloop 官网设计规范

版本：V1.0  
适用范围：首页、金融产品、解决方案、Finloop AI、资源中心、关于星路及后续详情页。

## 1. 设计方向

Finloop 官网应呈现“可信的机构金融能力 + 清晰的科技产品体验”。整体以明亮、克制、精确为主，深色用于建立对比、聚焦和科技感，但不把深蓝或黑色作为所有金融科技模块的默认答案。

参考 Ramp 多页面后提炼的可迁移原则：

- 大面积页面背景保持明亮，使用白色、冷白和浅灰蓝建立章节层次。
- 复杂产品通过圆角容器、真实界面、流程状态和局部色块表达，而不是依赖整块深色背景。
- 深色可用于 Hero、关键品牌时刻、产品演示、技术章节、CTA 或其他需要高对比的焦点模块。
- 卡片既是内容容器，也是视觉载体；应有明确的内部构图，不做只有标题和描述的空卡片阵列。
- 标题、描述、内容区严格遵循统一版心和纵向层级。
- 不复制参考网站的品牌色、文案、插图或页面结构；只吸收其层级、节奏、卡片和产品展示方法。

## 2. 页面骨架

### 2.1 版心

- 最大内容宽度：`1292px`
- 页面左右安全距离：`clamp(20px, 4.5vw, 64px)`
- 所有章节标题、描述和主体内容使用同一左边界。
- 全宽背景可以突破版心，内部内容不得突破版心。

### 2.2 章节间距

- 桌面端标准章节：`96-120px`
- 移动端标准章节：`64-76px`
- 标题到描述：`16-24px`
- 标题组到主体内容：`48-64px`
- 不通过连续空白制造“高级感”；每段空白必须对应清晰的信息分组。

### 2.3 标题结构

章节标题与描述统一采用上下排列：

1. 标题
2. 描述
3. 内容或交互组件

桌面端不再使用标题在左、描述在右的章节头部布局。

## 3. 色彩系统

### 3.1 基础色

- 页面底色：`#F3F6F8`
- 纯白表面：`#FFFFFF`
- 冷灰蓝表面：`#EAF0F4`
- 主文字：`#071423`
- 次级文字：`#5D707D`
- 弱边框：`#D6DEE4`
- 品牌蓝：`#0D7DF1`
- 品牌蓝悬停：`#086BD0`

### 3.2 深色使用节奏

深色背景适合用于：

- Hero 主视觉需要高对比时
- 页尾
- 重点产品演示或沉浸式技术场景
- 需要从浅色内容中形成明确章节转换的区域
- 强转化 CTA
- 真实产品截图自身为深色界面时

深色并非禁止项。单个页面可以出现多个深色区域，但应通过浅色章节、留白或不同表面进行呼吸和分隔；避免三个连续的大面积深色章节，也避免整页大多数卡片都使用同一种深蓝背景。

建议以页面视觉面积约 20%-35% 作为常见起点，再根据页面主题调整。AI、RWA 或技术基础设施页面可以适度提高，内容与产品目录页通常应更明亮。

产品列表、功能浏览器、流程图和普通信息卡默认先考虑浅色表面；当深色能强化真实产品界面、数据关系或章节重点时，可以有目的地使用。

## 4. 圆角与表面

### 4.1 圆角规格

- 小控件、标签：`8px`
- 按钮、输入框：`10px`
- 普通信息卡：`16px`
- 产品演示卡、媒体卡：`20px`
- Hero 内大型视觉容器：`24px`

同一组件内最多使用两级圆角，外层圆角应大于内层。

### 4.2 卡片表面

推荐优先级：

1. 白色卡片置于浅灰蓝页面背景
2. 浅灰蓝卡片置于白色页面背景
3. 品牌蓝作为局部状态、选中项或小面积强调
4. 深色卡片用于确有内容作用的重点场景，并与周围浅色表面形成节奏

默认卡片：

- `background: #FFFFFF`
- `border: 1px solid #D6DEE4`
- `border-radius: 16px`
- 默认无阴影，悬停可使用轻微阴影或 `translateY(-2px)`

禁止把阴影、渐变、玻璃模糊和深色同时堆在一张卡片上。

## 5. 卡片类型

### 5.1 产品卡

结构：类别或编号 → 标题 → 一句话价值 → 产品证据/类型 → CTA。

- 默认使用浅色圆角卡片。
- 产品界面或抽象视觉应占卡片面积的 40%-60%。
- 选中状态优先用边框、浅蓝底或局部品牌蓝，不整张变为深蓝。

### 5.2 产品浏览器

当前金融产品页的深色浏览器应逐步改为：

- 外层浅灰蓝或白色圆角容器
- 左侧标签使用白色/透明背景
- 选中项使用浅蓝底、品牌蓝文字或品牌蓝窄边
- 右侧详情使用白色卡片，并通过数据、产品类型、流程或小型视觉强化层次
- 移动端优先使用圆角 Accordion，而不是缩小桌面端的左右面板

### 5.3 数据卡

- 大数字作为视觉主体，描述紧随其后。
- 不把所有数据做成完全相同的四宫格。
- 允许一张主数据卡 + 多张辅助数据卡。

### 5.4 案例与内容卡

- 使用图片、品牌标记、结果摘要或内容分类构成识别度。
- 不使用只有标题、两行描述和箭头的通用卡片。
- 多卡片排列时允许大小、跨度或媒体比例变化，但对齐规则必须一致。

## 6. 排版

### 6.1 章节标题

- 字号：`clamp(36px, 3.4vw, 48px)`
- 行高：`1.16`
- 字重：`500`
- 字距：`-0.03em`
- 最大宽度：约 `900px`

### 6.2 章节描述

- 字号：`16px`
- 行高：`1.7`
- 最大宽度：`720px`
- 颜色：次级文字色

### 6.3 卡片标题

- 普通卡片：`20-24px`
- 重点产品卡：`28-36px`
- 不与章节标题争夺层级。

## 7. 产品视觉

- 优先使用真实产品界面、业务流程、数据状态、产品结构和项目专属抽象视觉。
- UI 截图应置于圆角设备框或产品画布中，并保留足够内边距。
- 抽象视觉只承担氛围与连接关系，不代替真实产品证据。
- 金币、现金堆、K 线、通用粒子球和无意义仪表盘不作为金融科技视觉。
- Web2 × Web3 的表达使用连接、迁移、组合和产品层级，不默认使用霓虹或赛博朋克语言。

## 8. 交互与动效

- 卡片悬停：120-180ms，轻微位移、边框或背景变化。
- 选项切换：200-300ms，以内容连续性为主。
- 圆角卡片内部的产品截图可以有局部状态变化，但普通阅读内容保持稳定。
- 不让动画延迟导航和主要操作。
- `prefers-reduced-motion` 下移除位移、循环和视差，保留立即可读的最终状态。

## 9. 响应式

- 桌面多列卡片在移动端重组为单列或双列，不横向压缩。
- 大型产品浏览器在移动端转换为 Accordion。
- 移动端圆角可以从 `20-24px` 收敛到 `14-18px`。
- 标题与描述保持上下结构。
- 所有控件保持至少 `44px` 的触控区域。

## 10. 页面使用规则

### 首页

- 允许 Hero 使用深色或影像背景。
- 后续主要章节以浅色圆角产品容器为主。
- 深色模块之间至少间隔一个浅色章节。

### 金融产品页

- Hero 可保留深色主视觉。
- 产品浏览器优先改为浅色圆角系统，也可保留局部深色产品演示区，但不再让整个组件从标签到详情全部处于同一深色平面。
- 产品来源、生命周期和 Web2 × Web3 使用不同浅色表面区分，不连续使用深色。
- RWA、虚拟资产等内容不因主题而默认变成霓虹深色。

### 解决方案与 AI 页面

- 用不同业务场景的产品演示卡、流程卡和案例卡组织内容。
- AI 可以使用更强的动态或数据视觉，但仍以浅色页面体系为基础。

## 11. 代码约束

新增页面和模块优先复用以下全局 token：

```css
--max: 1292px;
--edge: clamp(20px, 4.5vw, 64px);
--section-title-size: clamp(36px, 3.4vw, 48px);
--section-title-line: 1.16;
--section-copy-size: 16px;
--section-copy-line: 1.7;
--section-copy-width: 720px;
--card-radius: 16px;
--media-radius: 20px;
--control-radius: 10px;
```

禁止在新模块中重新手写另一套章节字号、版心宽度和圆角规格。确有视觉例外时，必须说明该例外的内容作用。

## 12. 参考研究范围

本规范综合参考 Ramp 的不同内容类型，而非只参考首页：

- 首页：品牌节奏、Hero 与产品叙事
- Products：产品分类、产品系统与功能层级
- Small Business：场景化段落与连续产品演示
- Startups：受众型页面、证据卡和产品组合
- Banking Partnerships：合作方案、能力卡与表单收口
- Applied AI Solutions：AI 服务叙事、问题拆解与方法说明
- Press / 内容页面：数据、内容卡和编辑型信息层级

最终视觉必须保持 Finloop 的品牌蓝、机构金融语境和真实业务内容，不复刻 Ramp 页面。
